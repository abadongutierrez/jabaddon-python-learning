# Enhance LLMs using RAG and Hugging Fac

## Setup

```
python3 -m venv myenv

source myenv/bin/activate

pip install transformers datasets torch faiss-cpu wget

pip install matplotlib scikit-learn
```